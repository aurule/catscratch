<% provide(:title, @user.name) %>
<%=
# gravatar_for @user

gravatar_id = Digest::MD5::hexdigest(@user.email.downcase)
gravatar_url = "https://secure.gravatar.com/avatar/#{gravatar_id}&d=mm".html_safe
debug(image_tag(gravatar_url, alt: @user.name, class: "gravatar"))

image_tag("http://example.com")
 %>
<h1>
  <%= @user.name %>
</h1>
